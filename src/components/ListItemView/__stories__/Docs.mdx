import {
    Meta,
    Markdown,
    Canvas,
    AnchorMdx,
    CodeOrSourceMdx,
    HeadersMdx,
} from '@storybook/addon-docs';
import * as Stories from './ListItemView.stories';
import Readme from '../README.md?raw';

{/* The page is assembled from the README: the placeholder tags of the examples are replaced with the live stories below */}
{/* The canvases hide the source generated by Storybook — the code of an example is the hand-written snippet of the README */}
{/* The props are documented by the table at the end of the README, so no generated args table is rendered here either */}

export const ListItemViewSizes = () => <Canvas of={Stories.Sizes} sourceState="none" />;
export const ListItemViewSlots = () => <Canvas of={Stories.Slots} sourceState="none" />;
export const ListItemViewStates = () => <Canvas of={Stories.States} sourceState="none" />;
export const ListItemViewSelection = () => <Canvas of={Stories.Selection} sourceState="none" />;
export const ListItemViewNesting = () => <Canvas of={Stories.Nesting} sourceState="none" />;
export const ListItemViewDragHandle = () => <Canvas of={Stories.DragHandle} sourceState="none" />;
export const ListItemViewCustomElement = () => (
    <Canvas of={Stories.CustomElement} sourceState="none" />
);

<Meta of={Stories} />

<Markdown
    options={{
        overrides: {
            code: CodeOrSourceMdx,
            a: AnchorMdx,
            ...HeadersMdx,
            ListItemViewSizes,
            ListItemViewSlots,
            ListItemViewStates,
            ListItemViewSelection,
            ListItemViewNesting,
            ListItemViewDragHandle,
            ListItemViewCustomElement,
        },
    }}
>
    {Readme}
</Markdown>
