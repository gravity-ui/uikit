import type {SelectOption, SelectOptionGroup} from './types';

export const Option = <T extends any>(_props: SelectOption<T>) => null;

export const OptionGroup = <T extends any>(_props: SelectOptionGroup<T>) => null;
