import {composeStories} from '@storybook/react';

import * as DefaultTextInputStories from '../__stories__/TextInput.stories';

export const TextInputStories = composeStories(DefaultTextInputStories);
