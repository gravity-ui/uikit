import {
    Meta,
    Markdown,
    Canvas,
    AnchorMdx,
    CodeOrSourceMdx,
    HeadersMdx,
} from '@storybook/addon-docs';
import * as Stories from './List.stories';
import Readme from '../README.md?raw';

{/* The page is assembled from the README: the placeholder tags of the examples are replaced with the live stories below */}
{/* The canvases hide the source generated by Storybook — the code of an example is the hand-written snippet of the README */}
{/* The props are documented by the tables at the end of the README, so no generated args table is rendered here either */}

export const ListExample = () => <Canvas of={Stories.Default} sourceState="none" />;
export const ListSizes = () => <Canvas of={Stories.Sizes} sourceState="none" />;
export const ListActions = () => <Canvas of={Stories.Actions} sourceState="none" />;
export const ListSections = () => <Canvas of={Stories.Sections} sourceState="none" />;
export const ListItemContent = () => <Canvas of={Stories.ItemContent} sourceState="none" />;
export const ListItemSlots = () => <Canvas of={Stories.ItemSlots} sourceState="none" />;
export const ListCustomMarkup = () => <Canvas of={Stories.CustomMarkup} sourceState="none" />;
export const ListLinks = () => <Canvas of={Stories.Links} sourceState="none" />;
export const ListControlledActivation = () => (
    <Canvas of={Stories.ControlledActivation} sourceState="none" />
);
export const ListSingleSelection = () => <Canvas of={Stories.SingleSelection} sourceState="none" />;
export const ListMultipleSelection = () => (
    <Canvas of={Stories.MultipleSelection} sourceState="none" />
);
export const ListVirtualized = () => <Canvas of={Stories.Virtualized} sourceState="none" />;
export const ListInteractiveRows = () => <Canvas of={Stories.InteractiveRows} sourceState="none" />;
export const ListFocusOwner = () => <Canvas of={Stories.FocusOwner} sourceState="none" />;

{/* The only drag-and-drop example of the page — @hello-pangea/dnd, the recommended library; the other libraries are linked from the README to their stories */}
{/* The Code panel of this example shows the complete source of its file: the integration is meant to be copied as is */}

export const ListDragAndDrop = () => <Canvas of={Stories.DragAndDrop} />;

<Meta of={Stories} />

<Markdown
    options={{
        overrides: {
            code: CodeOrSourceMdx,
            a: AnchorMdx,
            ...HeadersMdx,
            ListExample,
            ListSizes,
            ListActions,
            ListSections,
            ListItemContent,
            ListItemSlots,
            ListCustomMarkup,
            ListLinks,
            ListControlledActivation,
            ListSingleSelection,
            ListMultipleSelection,
            ListVirtualized,
            ListInteractiveRows,
            ListDragAndDrop,
            ListFocusOwner,
        },
    }}
>
    {Readme}
</Markdown>
