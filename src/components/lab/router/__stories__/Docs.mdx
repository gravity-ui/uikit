import {
    Meta,
    Markdown,
    Canvas,
    AnchorMdx,
    CodeOrSourceMdx,
    HeadersMdx,
} from '@storybook/addon-docs';
import * as Stories from './RouterProvider.stories';
import Readme from '../README.md?raw';

export const RouterProviderExample = () => <Canvas of={Stories.Default} sourceState="none" />;

<Meta of={Stories} />

<Markdown
    options={{
        overrides: {
            code: CodeOrSourceMdx,
            a: AnchorMdx,
            ...HeadersMdx,
            RouterProviderExample,
        },
    }}
>
    {Readme}
</Markdown>
