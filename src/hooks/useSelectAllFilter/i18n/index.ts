import {addComponentKeysets} from '../../../components/utils/addComponentKeysets';

import en from './en.json';
import ru from './ru.json';

const COMPONENT = 'Select all hook';

export default addComponentKeysets({en, ru}, COMPONENT);
